---
language: node_js
node_js:
  - 6
  - 8
cache:
  directories:
  - node_modules
  - functions/node_modules
install:
  - npm install
  - cd functions && npm install
script:
 - 'cd $TEST_DIR && npm install && npm test'
env:
  - TEST_DIR='./'
  - TEST_DIR='functions'
  